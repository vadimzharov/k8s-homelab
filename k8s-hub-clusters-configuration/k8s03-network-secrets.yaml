apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: k8s03-network-secrets
  namespace: argocd
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: k8s03-network-secrets
    creationPolicy: Owner
  data:
  - secretKey: nginxingressip
    remoteRef:
      key: clusters/k8s03/ingress
      property: nginxingressip
