apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: k8s02-network-secrets
  namespace: argocd
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: k8s02-network-secrets
    creationPolicy: Owner
  data:
  - secretKey: nginxingressip
    remoteRef:
      key: clusters/k8s02/ingress
      property: nginxingressip
  - secretKey: nginxexternalingressip
    remoteRef:
      key: clusters/k8s02/ingress
      property: nginxexternalingressip
